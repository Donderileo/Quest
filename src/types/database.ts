/**
 * PLACEHOLDER — substituir por tipos gerados:
 *   npm run db:types   (requer `npx supabase start` com Docker rodando)
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
